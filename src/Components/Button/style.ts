import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  btn: {
    width: "100%",
    padding: "15px",
    borderRadius: "8px",
    border: "0px",
    margin: "5px 0px",
    cursor: "pointer",
  },
});
